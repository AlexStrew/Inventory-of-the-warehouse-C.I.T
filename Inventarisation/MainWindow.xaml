<Window x:Class="Inventarisation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inventarisation"
        xmlns:hc="https://handyorg.github.io/handycontrol"

        xmlns:control="http://filterdatagrid.control.com/2021" 
        mc:Ignorable="d"
        >

    <Grid Background="AliceBlue" >
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid x:Name="Header" Height="30" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7  *"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <Button x:Name="NomenclatureBtn" Grid.Column="1" Margin="0 0 5 0">Справочник</Button>
                <Button x:Name="WriteOffBtn" Grid.Column="1" Margin="5 0 0 0">Списание</Button>
            </StackPanel>
            
        </Grid>
        <Frame x:Name="MainFrame" Grid.Row="1"></Frame>
    </Grid>
</Window>
