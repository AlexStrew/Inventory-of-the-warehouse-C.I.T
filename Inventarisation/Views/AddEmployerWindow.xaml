<Window x:Class="Inventarisation.Views.AddEmployerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inventarisation.Views"
        mc:Ignorable="d"
        Title="Добавление Сотрудника" Height="150" Width="200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="5">


            <TextBlock Text="Добавить сотрудника"/>
            <TextBox x:Name="EmployerTBox"/>

        </StackPanel>


        <Grid
            Grid.Row="1"
            Height="40"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom">
            <Button
                x:Name="AddEmployerBtn"
                Width="100"
                Margin="0,0,5,5"
                Click="AddEmployerBtn_Click">
                Добавить
            </Button>
        </Grid>
    </Grid>
</Window>
